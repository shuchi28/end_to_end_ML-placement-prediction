# -*- coding: utf-8 -*-
"""End_to_end _ML.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1gDqHLnaFgeOaP9W9ASM8TR11kzBBV1oN
"""

import pandas as pd
import numpy as np

df=pd.read_csv('/content/drive/MyDrive/Colab Notebooks/Copy of placement.csv')

df

df.head()

df.shape

df= df.iloc[:,1:]

"""#steps

# 0 preprocess+EDA+Feature selection
# 1 Extract imput and output cols
# 2 Scale the values
# 3 Train test split
# 4 Train the model
# 5 Evaluate the model selection
# 6 Deploy the model

# Data preprocessing
"""

df.info()

df.head()

import matplotlib.pyplot as plt

plt.scatter (df['cgpa'],df['iq'],c=df['placement'])

"""# Extract an input and output column"""

X= df.iloc[:,0:2]
Y= df.iloc[:,-1]

X

Y.shape

# Train test split

from sklearn.model_selection import train_test_split

train_test_split(X,Y,test_size=0.1)

from sklearn.model_selection import train_test_split

X_train,X_test,Y_train,Y_test=train_test_split(X,Y,test_size=0.1)

X_train

Y_train

X_test

Y_test

from sklearn.preprocessing import StandardScaler

scaler=StandardScaler()

X_train=scaler.fit_transform(X_train)

X_train

X_test=scaler.transform(X_test)

X_test

from sklearn.linear_model import LogisticRegression

clf=LogisticRegression()

clf.fit(X_train,Y_train)

Y_pred=clf.predict(X_test)

clf.predict(X_test)

Y_test

from sklearn .metrics import accuracy_score

accuracy_score(Y_test,Y_pred)

from mlxtend.plotting import plot_decision_regions

plot_decision_regions(X_train,Y_train.values,clf=clf,legend=2)

import pickle

pickle.dump(clf,open('model.pkl','wb'))